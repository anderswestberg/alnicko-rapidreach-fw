# MQTT Shell Backend Configuration Overlay
# This overlay customizes the MQTT shell backend client ID

# Set a custom hostname that might be used in client ID generation
CONFIG_NET_HOSTNAME_ENABLE=y
CONFIG_NET_HOSTNAME="rrsa"

# Enable network shell for debugging
CONFIG_NET_SHELL=y
